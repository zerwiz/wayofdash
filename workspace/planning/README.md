# Planning (`workspace/planning/`)

**Purpose:** UI and product planning notes that live beside other workspace content (`docs/`, `notes/`, etc.) and appear in the dev file tree like any other Markdown.

**Contents:**

- `ui-planning-document.md` — main UI/planning doc for this app.

**Setup:** None beyond the Way of UI dev server; open files from the sidebar under `planning/`.

**Ports/APIs:** Same as the app (`workspace/` is served via the Vite data-tree APIs in dev).
